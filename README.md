# VIC

Initial repository structure for the VIC platform.

## Structure

- `frontend/`: React + Vite client application
- `backend/`: backend service directory placeholder

## Frontend

```bash
cd frontend
npm install
npm run dev
```

Default environment variables are in `frontend/.env`.

## Backend

Backend source code was not present in the current workspace at the time of this initial push.
